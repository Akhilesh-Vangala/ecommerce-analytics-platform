.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.env

# Raw data is downloaded via etl/download_data.py (Kaggle), not committed
data/raw/*.csv
data/processed/*.csv

# Dashboard extracts are generated via etl/export_dashboard_extracts.py, not committed
dashboard/extracts/*.csv

.DS_Store

# Personal working notes and generator scripts, not part of the published project
local_reference/
